interface:
  display_name: "Parallel Agent Handoff"
  short_description: "Coordinate agents with SQLite and OpenCode"
  default_prompt: "Use $parallel-agent-handoff to create shared tasks and dispatch OpenCode agents safely."
