interface:
  display_name: "Parallel Agent Handoff"
  short_description: "Coordinate Codex sessions with SQLite tasks"
  default_prompt: "Use $parallel-agent-handoff to coordinate backend and frontend work through SQLite-backed shared tasks."
